{
  "version": 2,
  "builds": [
    {
      "src": "backend/server.js", // Change to your entry file
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/index.js"  // This should match your API folder/file
    }
  ]
}
